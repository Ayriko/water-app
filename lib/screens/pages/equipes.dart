import 'package:flutter/material.dart';
class Equipes extends StatelessWidget {
  const Equipes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Les equipes !'));
  }
}